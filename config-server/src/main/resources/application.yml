server:
  port: 8887
spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/lmonkiewicz/config-server-example"
          search-paths:
            - 'config/{application}/'
          default-label: 'main'
