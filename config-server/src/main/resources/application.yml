server:
  port: 8888 # default port for Spring Cloud Config Server
spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/lmonkiewicz/config-server-example"
          search-paths:
            - 'config/{application}'
          default-label: 'main'
          clone-on-start: true

